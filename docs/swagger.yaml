info:
  contact: {}
  license: {}
paths:
  /api/user/register:
    post:
      parameters:
      - description: Name
        in: body
        name: name
        required: true
        schema:
          type: string
      - description: State
        in: body
        name: state
        schema:
          type: integer
      - description: CreatedBy
        in: body
        name: created_by
        schema:
          type: integer
      produces:
      - application/json
      responses:
        "200": {}
        "500": {}
      summary: Allows a new usr to register for the service
swagger: "2.0"
